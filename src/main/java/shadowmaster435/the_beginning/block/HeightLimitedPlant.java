package shadowmaster435.the_beginning.block;

public class HeightLimitedPlant {
    public static int counter;
    /*

    public HeightLimitedPlant(Settings settings) {
        super(settings);
    }
    public static Block block = null;
    public boolean height(BlockState state, WorldView world, BlockPos pos, World world2) {
        for (counter = -1; counter >= -5; ) {
            --counter;
            if (world.getBlockState(pos.offset(Direction.DOWN, counter)) == state.getBlock(Blocks.OBSIDIAN.getDefaultState()))

            {

            } else{
            }
        }
    }
    public boolean canPlaceAt(BlockState state, WorldView world, BlockPos pos) {
        return height();
    }
    public void onBlockAdded(BlockState state, World world, BlockPos pos, BlockState oldState, boolean notify) {
        world.getBlockTickScheduler().schedule(pos, this, 2);
    }
    public void scheduledTick(BlockState state, ServerWorld world, BlockPos pos, Random random) {
        world.getBlockTickScheduler().schedule(pos, this, 2);

    }

     */
}
